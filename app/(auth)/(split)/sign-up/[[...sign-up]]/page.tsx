import type { Metadata } from "next";

export const dynamic = "force-dynamic";

export const metadata: Metadata = { title: "Create Account · CampusHire" };

// Rendered by AuthShell in app/(auth)/(split)/layout.tsx.
export default function SignUpCatchAllPage() {
  return null;
}
