import type { Metadata } from "next";

export const dynamic = "force-dynamic";

export const metadata: Metadata = { title: "Sign In · CampusHire" };

// Rendered by AuthShell in app/(auth)/(split)/layout.tsx.
export default function SignInCatchAllPage() {
  return null;
}
